<h1>Artists</h1>
<ul>
  <% @artists.each do |artist| %>
    <li>

    <%= link_to artist.name, artist_path(artist) %>
    (<%= artist.songs.count %> <%= "songs".pluralize(artist.song_count) %>)
    </li>

  <% end %>
</ul>