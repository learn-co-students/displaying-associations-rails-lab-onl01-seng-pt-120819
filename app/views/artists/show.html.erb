
<h1><%= @artist.name %></h1>
<h2><%= pluralize(@artist.song_count, 'song') %></h2>
<ul>
    <% @artist.songs.each do |f| %>
        
        <li><%= link_to f.title, song_path(f) %></li>
    <% end %>
</ul>