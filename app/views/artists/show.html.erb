<h1><%= @artist.name %></h1>

<h3><%= pluralize(@artist.song_count, 'Song') %></h3>
<ul>
    <% @artist.songs.each do |s| %>
        <p><%= link_to s.title, song_path(s) %></li></p>
    <% end %>
</ul>